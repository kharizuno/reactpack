import React from "react";

const CircularProgress = ({className = 'abs-center'}) => <div className={`${className}`}>
    <div className={`loader`}></div>
</div>;

export default CircularProgress;
